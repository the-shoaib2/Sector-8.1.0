{
  "name": "@synapse/common",
  "version": "1.0.0",
  "description": "Shared types, utilities, and constants for Synapse platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "test": "vitest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "zod": "^3.22.4",
    "uuid": "^9.0.1",
    "date-fns": "^2.30.0"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.7",
    "typescript": "^5.3.0",
    "rimraf": "^5.0.5"
  }
}

